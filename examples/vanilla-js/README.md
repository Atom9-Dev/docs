# Vanilla JS Example

This is a minimal client-side example showing how to trigger Atom9 onboarding/test auth endpoints during early integration.

## Run

Serve this folder as static files, then open `index.html`.
